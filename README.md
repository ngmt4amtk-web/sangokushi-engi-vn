# 三国志演義 読み物版 VN（全120回）

報告調の復元ナレ＋会話の二層で全120回を一本道ノベルゲーム化。焚天/関羽伝エンジン再利用。
- `scenes/scenes_NNN.js` … 各回のビート（読み物版mdから `_tools/md2scenes.py` で決定論生成）
- `assets/sprites/` … 立ち絵（緑→透過切り抜き済）。`画像指示_codex.md` をcodexに投げて増分生成。
- 背景未生成時はmood色補正グラデで成立（ステージ0）。
